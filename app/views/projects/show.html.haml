%h1= "\"#{@project.name}\" Results"

- methodologies = Methodology.order('name')

.project-results
  .result-methods
    - methodologies.each_with_index do |method, i|
      = link_to '', class: i == 3 ? 'method-item active' : 'method-item' do
        -# = "#{i + 1}. "
        = method.name


  .result-detailed
    .method-title
      %h2= methodologies[3].name
      %h2 5.1

    %table.projects-list
      %thead
        %tr
          %th
            Parameter
          %th
            Value
          %th
            Score
      %tbody
        - Parameter.order("RANDOM()").all.each do |parameter|
          - s = parameter.default_weight / 10.0
          %tr{class: s > 8 ? 'green' : (s > 4 ? 'yellow' : (s > 1.5 ? 'red' : 'super-red'))}
            %td
              = parameter.name
            %td
              = parameter.possible_values.sample.value
            %td
              = s
